name: Preview

# This workflow is disabled by default.
# Vercel automatically creates preview deployments for PRs when connected to GitHub.
# Enable this only if you need custom PR preview behavior.

on:
  workflow_dispatch:

jobs:
  preview:
    name: Manual Preview Deploy
    runs-on: ubuntu-latest
    steps:
      - run: echo "Vercel handles PR previews automatically via GitHub integration"
